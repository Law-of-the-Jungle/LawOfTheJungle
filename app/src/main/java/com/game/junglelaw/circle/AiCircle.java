package com.game.junglelaw.circle;

/**
 * Created by apple on 11/30/15.
 */
public class AiCircle extends MovableCircle {

    // instead of MovableCircle should use this class as the raider, stay tuned!
    public AiCircle(float x, float y, float radius, int color, String difficulty) {
        super(x, y, radius, color, difficulty);
    }
}
